/*Escribir un algoritmo que identifique si una palabra es palíndromo.

Ejemplo: ANILINA

Utilizar Funciones*/

let nuevapalabra;

export const palíndromo=(palabra)=>{
 for(let i=0;i<=palabra.legth;i++){
    if(palabra.charAt(i)!= ""){
        nuevapalabra = palabra;
    }
    
}return(nuevapalabra);
}

