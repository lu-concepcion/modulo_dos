// do{
//     index = 1;
//     var item = prompt("Ingrese " + index + "° lenguaje programación :");
//     index ++;

// }while(index >= 4);

var index = 1;
for (var i=0; i < index; i++) {
    var item = prompt("Ingrese " + index + "° lenguaje programación :");
    index ++;
    if(item == "Python"){
        
        
    }
}

