var contador = 0;
let nombres = new Object();
let edades = new Object();

do {
    var nombre = prompt("Introduce tu nombre:");
    if ((nombre !== "" && isNaN(nombre)) && (nombre.toLowerCase !== "salir")) {
        nombres.
    }
}while(!paso);