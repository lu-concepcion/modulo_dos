
// Escriba un algoritmo que extraiga de un texto, todo aquello que se encuentre entre paréntesis, ej:

// Hola (mundo) que (tal) estas -> mundo tal 

import{ extraerTexto  } from '../funciones.js';  // importar sin nombre {} para que despues sugiera



let texto="Hola (mundo) q(ue (tal) estas( 34 (2)(4) hgh";




alert(extraerTexto(texto))