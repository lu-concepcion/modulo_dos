//generar datos de personas
let personas = [];
persona.rut;
persona.nombre;

const guardar = pers => {
    personas.rut = pers.rut;
    personas.nombre = pers.nombre;
    personas.fechaNac = pers.fechaNac;
    personas.edad = pers.fechaNac;
}

const obtenerPersona = (run) => {
    return persona;
}

module.exports={
    guardar, obtenerPersona
}