var contador = 0;
var numerosPares = [];

do {
    var p = prompt("ingrese un numero par");
    
    if ( p %2 == 0 && p>=2 && p<=20){
       numerosPares.push(p)
    }
    }while(numerosPares.length < 5);

do {
    
}